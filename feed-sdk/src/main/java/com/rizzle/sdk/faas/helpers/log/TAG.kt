package com.rizzle.sdk.faas.helpers.log

class TAG {
    companion object {
        const val DOWNLOAD_FILE = "DOWNLOAD_FILE"
        const val PRE_FETCH = "PRE_FETCH"
        const val DB_OPERATION = "DB_OPERATION"
        const val FEED = "FEED"
    }
}